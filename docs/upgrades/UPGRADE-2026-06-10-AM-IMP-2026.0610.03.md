# UPGRADE 2026-06-10 AM-IMP-2026.0610.03

## Summary

Installed the AMCore task body evidence log standard for HOZO AM.

This upgrade converts HOZO AM total-control task pages so `內文` becomes the
primary append-only evidence and judgment log. The separate `來源原文` property is
kept only as a compatibility note.

## Scope

- Project: HOZO AM
- AMCore package: `D:\Codex_project\AM_Core\versions\AM-IMP-2026.0610.03`
- Target data source: HOZO AM project-local `HOZO 總控任務庫`

## Changes Applied

- Converted 51 HOZO AM total-control tasks to the `任務控制紀錄` body format.
- Preserved existing task fields in:
  - `目前任務摘要`
  - `最新判斷`
  - `證據與處理紀錄`
- Moved existing `來源原文` property content into the task body where available.
- Preserved prior page body content under `舊內文封存`.
- Updated each task's `來源原文` property to a compatibility message.
- Updated hourly LINE reconciliation task writes so new tasks are created with
  the `任務控制紀錄` body format.
- Updated hourly LINE reconciliation task updates so evidence is appended as a
  new `證據與處理紀錄` entry instead of being written only to `來源原文`.
- Updated `config/hourly-line-task-reconciliation.json` to require
  AM-IMP-2026.0610.03 task body writes.
- Added the AMCore batch tool:
  - `D:\Codex_project\AM_Core\tools\apply-task-body-evidence-log-standard.js`

## Verification

Commands run from AMCore:

```text
node --check tools\apply-task-body-evidence-log-standard.js
node tools\apply-task-body-evidence-log-standard.js HOZO_AM --write
node tools\apply-task-body-evidence-log-standard.js HOZO_AM
node --check D:\Codex_project\HOZO_AM\line-oa-webhook\scripts\sync-line-message-judgements.js
```

Final result:

```text
totalTasks: 51
alreadyStandard: 51
wouldConvert: 0
failed: 0
```

Smoke result:

- One non-actionable HOZO LINE message was processed by the hourly script.
- No task was created or updated.
- The message was marked judged.

## Data Boundary

No SevenAM data was copied into HOZO AM. No HOZO AM task data was copied into
AMCore. AMCore only stores the shared standard and the reusable migration tool.

## Follow-Up

Future task creation, LINE reconciliation, meeting sync, and report sync should
write new evidence directly into `內文` using the AM-IMP-2026.0610.03 format.
