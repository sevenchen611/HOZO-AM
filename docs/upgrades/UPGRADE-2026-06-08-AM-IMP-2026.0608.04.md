# Upgrade Version Record

Version: AM-IMP-2026.0608.04

Improvement name: Cron report deployment verification

Target project: HOZO AM

Upgrade date: 2026-06-08

Owner: Project-local HOZO AM controller

## Purpose

Create a project-local daily report snapshot trail so scheduled reports can be audited from creation, sending, and controller confirmation.

## Scope

| Item | Included? | Notes |
| --- | --- | --- |
| Code changes | Yes | `src/control-api.js` writes daily report snapshots and confirmation updates. |
| Documentation changes | Yes | Manifest and upgrade record updated. |
| Environment variables | Yes | Uses HOZO-prefixed snapshot data source env var. |
| Notion schema changes | Yes | HOZO daily report snapshot data source was created locally. |
| Render sync/deploy | Pending | Requires production Render env var and deploy verification. |
| LINE behavior changes | No | Report send flow is unchanged except snapshot recording. |
| Scripts | Yes | `scripts/setup-daily-report-snapshots.js`. |

## Files Changed

| File | Change |
| --- | --- |
| `src/control-api.js` | Added snapshot creation and confirmation update for daily reports. |
| `scripts/setup-daily-report-snapshots.js` | Creates or verifies the HOZO snapshot data source. |
| `.env.example` | Documents the HOZO snapshot env var name only. |
| `render.yaml` | Adds Render env var placeholder. |

## Environment Variables

| Env var | Required? | Project-specific? | Notes |
| --- | --- | --- | --- |
| `HOZO_DAILY_REPORT_SNAPSHOTS_DATA_SOURCE_ID` | Yes | Yes | Stored only in HOZO local/Render environment. |

## Data Isolation Check

| Check | Result |
| --- | --- |
| Uses only this project's LINE channel | Pass |
| Uses only this project's Notion data sources | Pass |
| Uses only this project's Render service | Pass |
| Does not copy secrets from another project | Pass |
| Does not write to another business unit's database | Pass |

## Verification

| Step | Expected Result | Actual Result |
| --- | --- | --- |
| Syntax check | Server and control API parse cleanly. | Passed. |
| Local health check | Snapshot env var exists locally. | Passed. |
| Feature test | Snapshot data source exists in HOZO Notion. | Passed. |
| Production deploy/sync | Render has env var and deployed code. | Pending. |
| Production health check | `/control/health` reports snapshot configured. | Pending. |

## Rollback Plan

Remove the snapshot env var from Render/local env and revert `src/control-api.js` snapshot calls. Existing HOZO snapshot records can remain as audit history.

## Final Status

Installed
