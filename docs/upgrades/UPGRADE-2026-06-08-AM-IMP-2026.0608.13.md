# UPGRADE-2026-06-08-AM-IMP-2026.0608.13

Project: HOZO AM

Status: Installed

## Summary

HOZO AM now has the judgment calibration knowledge-base workflow installed with project-local Notion calibration databases, LINE command handling, and a HOZO-specific calibration script.

## Installed Scope

| Area | Result |
| --- | --- |
| Project-local databases | Created HOZO judgment rules and calibration cases data sources. |
| Environment | Stored HOZO-prefixed calibration data source IDs in the HOZO local environment only. |
| LINE command mode | Added start, pause, status, and controller-reply handling to `src/server.js`. |
| Review target | Verified the HOZO review target can be resolved from HOZO LINE group members. |
| Render config | Added HOZO calibration env var placeholders to `render.yaml`. |

## Data Boundary

Keep HOZO AM task records, LINE target IDs, controller replies, Notion database IDs, and secrets inside HOZO AM only. No SevenAM records or IDs were copied.

## Verification

| Step | Result |
| --- | --- |
| `node --check src/server.js` | Passed. |
| `node --check scripts/judgment-calibration.js` | Passed. |
| Calibration database setup | Passed; HOZO-local data sources created. |
| Review target lookup | Passed; target found from HOZO LINE group members with masked ID only. |
| Production deploy | Pending Render deploy and live LINE command verification. |

## Rollback

Remove the HOZO calibration env vars from local/Render environments and revert `src/server.js`, `scripts/judgment-calibration.js`, `package.json`, `.env.example`, and `render.yaml` changes for this version.
