# UPGRADE-2026-06-13-AM-IMP-2026.0613.03

Project: HOZO AM

Version: `AM-IMP-2026.0613.03`

## Summary

Local-worker schedule consolidation, Codex-only flavor: the 24/7 worker is HOZO AM's ONLY scheduler. Render hosts nothing but the web service (webhook intake, report pages, dashboard, control API) — zero cron jobs. No fallback exists by design: if the worker machine dies, HOZO AI judgment and reports stop until it restarts (this is the deliberate contrast with SevenAM, which keeps API-fallback crons).

## Changes

- `scripts/local-worker.js` timetable: extraction + triage every ~90s cycle; Next Action scheduled actions every 15 min; meeting-action and responsibility-candidate syncs hourly; the five daily reports at 08:30 / 10:00 / 13:00 / 17:00 / 20:30 through `scripts/render-cron-report.js` → webhook `/control/reports/send` (30-minute grace window, missed windows skipped, never back-filled); nightly 22:20 proposals and 22:45 feedback sync.
- render.yaml: all 8 cron jobs removed (user deleted them manually in the Render dashboard — Blueprint sync never deletes).
- Operating hours Taipei 07:00-23:00 apply to everything via the worker gate.
- Attachment parsing remains disabled (needs the Anthropic API for vision); attachments stay queued as 待轉檔.

## Environment Variables (names only)

CONTROL_API_URL (worker-driven report sends; already present in HOZO .env)

## Data Isolation Check

The worker talks only to the HOZO webhook and HOZO Notion sources.

## Verification

Worker restarted with the consolidated timetable; codex self-test and the operating-hours gate verified in the log. First full timetable day (worker-driven reports) verifies on 2026-06-13. All 8 Render crons confirmed deleted by the user.

## Status

Installed on the worker machine; Render side confirmed (Resources shows only `hozo-am-line-oa-webhook`). Repo commit f5f1d66.
