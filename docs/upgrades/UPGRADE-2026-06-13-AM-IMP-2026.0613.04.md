# UPGRADE-2026-06-13-AM-IMP-2026.0613.04

Project: HOZO AM

Version: `AM-IMP-2026.0613.04`

## Summary

Queue-database plan policy: the event queue Postgres must live next to the webhook in the cloud — never on the worker machine, because the queue's reliability must exceed what it protects. HOZO's test-period queue uses the Render account's single free-Postgres slot, released by upgrading SevenAM's production queue to a paid plan.

## Changes

- render.yaml: `hozoam-queue-db` (plan: free) + `DATABASE_URL` auto-wired into the web service via fromDatabase. Upgrade to `basic-256mb` in one line when HOZO productionizes (free tier expires after 30 days).
- Render constraints documented from live failures: max 25 services/databases per workspace; only ONE active free-tier Postgres per account; Blueprint sync creates/updates but never deletes (manual deletion required).
- Sequencing requirement: SevenAM's Blueprint must sync first (its queue upgrades to basic-256mb, freeing the slot) before HOZO's sync can create this database.

## Environment Variables (names only)

DATABASE_URL (auto-wired by Blueprint)

## Data Isolation Check

HOZO has its own database; no shared connection strings with SevenAM.

## Verification

Applied via Blueprint sync by the user in the Render dashboard (after the SevenAM upgrade). Webhook health (`/health` → `eventQueue`) verifies once the new web service deploy is live.

## Status

Pushed (repo commit fd67023); Render-side creation performed by the user during sync.
