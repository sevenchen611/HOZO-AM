# UPGRADE-2026-06-11-AM-IMP-2026.0610.05

Project: HOZO AM

Version: `AM-IMP-2026.0610.05`

## Summary

Installed the conversation-master task intake source rule into HOZO AM.

Hourly LINE task judgment now reads the LINE conversation master instead of the raw Message Data table. Message Data remains only for raw event logging, outgoing logging, attachment audit, resend tracing, and debugging.

## Changes

- Updated hourly LINE judgment to read `HOZO_CONVERSATIONS_DATA_SOURCE_ID`.
- Added conversation-level judgment fields to the HOZO LINE conversation master.
- Added the HOZO LINE group member index data source.
- Added webhook member-index capture for group and room speakers.
- Updated group option and responsibility owner narrowing flows to read the member index instead of Message Data.
- Updated User UI generation so conversation-led evidence is the default.
- Added `line:group-members` and `setup:line-group-members` project scripts.

## Verification

- Syntax checks passed for server, hourly judgment, group option sync, group member index sync, setup scripts, User UI generator, and meeting sync.
- HOZO hourly judgment dry-run reported `sourceDataSource: HOZO_CONVERSATIONS_DATA_SOURCE_ID`.
- HOZO hourly judgment dry-run reported `conversationContextLimit: 20`.
- Group option dry-run scanned conversation master records and read the member index.
- LINE member-list API returned `403 Access to this API is not available for your account`; webhook member-index capture is installed as the fallback.

## Status

Installed locally. Render deployment and live webhook verification remain project-local follow-up.
