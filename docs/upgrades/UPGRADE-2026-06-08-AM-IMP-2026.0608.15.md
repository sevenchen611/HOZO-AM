# Upgrade Record: AM-IMP-2026.0608.15

Project: HOZO AM

Improvement: Total-control task title hygiene

Status: Installed

## What Changed

- Updated LINE-derived task title generation so Notion page IDs, LINE IDs, message IDs, and sync IDs do not appear in `任務名稱`.
- Added `scripts/clean-total-control-task-titles.js`.
- Added `npm run tasks:clean-titles`.

## Verification

| Check | Result |
| --- | --- |
| Syntax | `node --check scripts/sync-line-message-judgements.js` passed. |
| Cleanup syntax | `node --check scripts/clean-total-control-task-titles.js` passed. |
| Existing cleanup | Found 0 matching HOZO AM task titles to update. |
| Final scan | Matched 0 task titles containing technical IDs. |

## Data Boundary

All checks used only the HOZO AM project-local total-control task database.

