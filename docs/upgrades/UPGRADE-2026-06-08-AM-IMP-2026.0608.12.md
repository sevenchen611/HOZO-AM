# Upgrade Version Record

Version: `AM-IMP-2026.0608.12`

Improvement name: Cron report reliability upgrade

Target project: HOZO AM

Upgrade date: 2026-06-08

Owner: HOZO AM / Codex

## Purpose

Prepare HOZO AM to handle temporary Render web service failures during scheduled report sends.

The 2026-06-08 17:00 `hozo-am-followup-afternoon` cron run reached `/control/reports/send`, but the web service returned `502` with a Render HTML error page. This version is ready for installation to add health ping, retry, timeout, and structured attempt logs.

## Scope

| Item | Included? | Notes |
| --- | --- | --- |
| Code changes | Yes | Installed shared retry/health-ping cron sender into HOZO AM. |
| Documentation changes | Yes | Manifest and upgrade record updated. |
| Environment variables | Yes | Added HOZO AM-local non-secret Render cron env values. |
| Notion schema changes | No | No database changes. |
| Render sync/deploy | No | Blueprint sync/deploy still needs production confirmation. |
| LINE behavior changes | Yes | Failure alert is sent only after final retry fails. |
| Scripts | Yes | Shared script installed from AMCore package. |

## Files Changed

| File | Change |
| --- | --- |
| `scripts/render-cron-report.js` | Added health ping, retry, timeout, structured attempt logs, and generic HOZO header/env prefix support. |
| `render.yaml` | Added non-secret retry, timeout, health ping, and prefix env values for report cron jobs. |
| `docs/project-improvement-manifest.md` | Added `Installed` row for this version. |
| `docs/upgrades/UPGRADE-2026-06-08-AM-IMP-2026.0608.12.md` | Added this installation record. |

## Environment Variables

| Env var | Required? | Project-specific? | Notes |
| --- | --- | --- | --- |
| `CONTROL_API_URL` | Yes | Yes | HOZO AM report send endpoint. |
| `CONTROL_LINE_PUSH_URL` | Yes for alerts | Yes | HOZO AM line push endpoint. |
| `HOZO_CONTROL_API_KEY` | Yes | Yes | HOZO AM secret; do not copy to AMCore or 7AM. |
| `AM_PROJECT_ENV_PREFIX` | Recommended | Yes | Use `HOZO`. |
| `AM_CONTROL_HEADER_PREFIX` | Recommended | Yes | Use `hozo`. |
| `AM_CRON_RETRY_DELAYS_MS` | Recommended | No | Shared default `10000,30000,60000`. |
| `AM_CRON_REQUEST_TIMEOUT_MS` | Recommended | No | Shared default `45000`. |
| `AM_CRON_HEALTH_PING_ENABLED` | Recommended | No | Shared default `true`. |
| `AM_CRON_ALERTS_ENABLED` | Recommended | No | Shared default `true`. |

## Data Isolation Check

| Check | Result |
| --- | --- |
| Uses only this project's LINE channel | Passed by configuration; Render values point to HOZO AM endpoints and HOZO AM key source. |
| Uses only this project's Notion data sources | Passed; no Notion data changes. |
| Uses only this project's Render service | Passed locally; Blueprint uses HOZO AM Render service URLs. |
| Does not copy secrets from another project | Passed. |
| Does not write to another business unit's database | Passed; no database writes. |

## Verification

| Step | Expected Result | Actual Result |
| --- | --- | --- |
| AMCore package check | Package is complete. | Passed: `AM-IMP-2026.0608.12` package check completed. |
| Syntax check | Shared script parses. | Passed for AMCore runtime template and packaged reference script. |
| Local feature test | Retry and health ping work against HOZO AM-local endpoints. | Script installed; live endpoint test not run to avoid sending report unexpectedly. |
| Production deploy/sync | Render cron uses HOZO AM env and succeeds after transient failures. | Not deployed yet. |
| Production health check | `/control/health` responds before send. | Not deployed yet. |

## Rollback Plan

If behavior is wrong, restore the previous HOZO AM `scripts/render-cron-report.js`, remove the new `AM_CRON_*` and prefix env values from HOZO AM cron jobs, then retest with HOZO AM's own control key.

## Final Status

Installed
