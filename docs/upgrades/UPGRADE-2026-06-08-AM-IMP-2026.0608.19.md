# UPGRADE 2026-06-08 AM-IMP-2026.0608.19

## Summary

Installed the AMCore total-control task table source text hide rule.

## Local Changes

- Confirmed HOZO AM `總控任務庫` `Default view` does not display `來源原文`.
- Reapplied the Default view display property list without `來源原文`.
- Kept the `來源原文` property in the database schema.
- Did not erase existing source text values.

## Verification

- Notion view update returned the Default view display property list without `來源原文`.
- The HOZO AM task data source still includes the `來源原文` property.

## Rollback

Add `來源原文` back to the HOZO AM Notion table view if the controller wants the source text visible in table headers again.
