# Upgrade Version Record

Version: `AM-IMP-2026.0608.10`

Improvement name: Notion database view layout registry

Target project: HOZO AM

Upgrade date: 2026-06-08

Owner: HOZO AM / Codex

## Purpose

Define and apply the first durable Notion database table layout for AM-style projects.

This version applies the shared LINE group options default table layout to HOZO AM.

## Scope

| Item | Included? | Notes |
| --- | --- | --- |
| Notion view update | Yes | Updated the HOZO LINE group options Default view. |
| Notion schema update | Yes | Added project-local `候選來源權責項目` relation because HOZO did not have it yet. |
| AMCore definition | Yes | Layout stored in AMCore config and upgrade package. |
| Secrets or live data | No | No tokens, customer records, or project data were stored in AMCore. |
| Render deploy | No | This is a Notion view/layout governance change only. |

## Applied Layout

Visible columns in order:

1. `總控專案`
2. `群組顯示名稱`
3. `LINE對話名稱`
4. `候選來源權責項目`

## Data Isolation Check

| Check | Result |
| --- | --- |
| Uses only HOZO AM Notion objects | Passed. The added relation targets HOZO AM responsibility data only. |
| Copies SevenAM data | Passed. No SevenAM row data or IDs were copied into HOZO records. |
| Stores secrets in AMCore | Passed. AMCore stores only logical layout definitions and property names. |

## Verification

| Step | Actual Result |
| --- | --- |
| Notion schema | `候選來源權責項目` exists on the HOZO LINE group options database. |
| Notion view | Default view display properties are `總控專案`, `群組顯示名稱`, `LINE對話名稱`, `候選來源權責項目`. |
| AMCore package check | `AM-IMP-2026.0608.10` package completeness check passed. |

## Rollback Plan

Restore the previous HOZO Notion Default view display settings if needed.

Do not delete `候選來源權責項目` if users have started filling it in; hide it from the view instead.

## Final Status

Installed
