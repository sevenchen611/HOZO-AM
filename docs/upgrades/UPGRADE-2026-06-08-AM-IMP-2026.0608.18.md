# Upgrade Version Record

Version: AM-IMP-2026.0608.18

Improvement name: Hourly LINE task reconciliation

Target project: HOZO AM

Upgrade date: 2026-06-08

Owner: Project-local HOZO AM controller

## Purpose

Make hourly LINE judgement reconcile new messages against same-conversation context and active tasks before creating new event-level tasks.

## Scope

| Item | Included? | Notes |
| --- | --- | --- |
| Code changes | Yes | Hourly script command includes outgoing group context. |
| Documentation changes | Yes | `AGENTS.md` documents reconciliation duty. |
| Environment variables | No | Uses existing HOZO message/task/report env vars. |
| Notion schema changes | No | Uses existing task and progress report sources. |
| Render sync/deploy | Pending | Requires Render Blueprint cron sync/deploy verification. |
| LINE behavior changes | Yes | New messages are judged against existing tasks before new task creation. |
| Scripts | Yes | `scripts/sync-line-message-judgements.js`. |

## Files Changed

| File | Change |
| --- | --- |
| `config/hourly-line-task-reconciliation.json` | Added shared reconciliation contract. |
| `package.json` | Updated hourly assistant command. |
| `render.yaml` | Added hourly LINE judgement cron. |
| `AGENTS.md` | Documents reconciliation responsibility and schedule. |

## Environment Variables

| Env var | Required? | Project-specific? | Notes |
| --- | --- | --- | --- |
| `HOZO_MESSAGES_DATA_SOURCE_ID` | Existing | Yes | Existing message source. |
| `HOZO_TASKS_DATA_SOURCE_ID` | Existing | Yes | Existing task source. |
| `HOZO_PROGRESS_REPORTS_DATA_SOURCE_ID` | Existing | Yes | Existing progress report source. |

## Data Isolation Check

| Check | Result |
| --- | --- |
| Uses only this project's LINE channel | Pass |
| Uses only this project's Notion data sources | Pass |
| Uses only this project's Render service | Pass |
| Does not copy secrets from another project | Pass |
| Does not write to another business unit's database | Pass |

## Verification

| Step | Expected Result | Actual Result |
| --- | --- | --- |
| Syntax check | Judgement script parses cleanly. | Passed. |
| Local health check | Contract JSON parses. | Passed. |
| Feature test | Hourly command includes outgoing group context. | Passed by package/script inspection. |
| Production deploy/sync | Render cron exists and runs. | Pending. |
| Production health check | Next cron run completes without creating duplicate tasks. | Pending. |

## Rollback Plan

Remove the hourly cron from `render.yaml` and revert the assistant hourly command to the previous script chain.

## Final Status

Installed
