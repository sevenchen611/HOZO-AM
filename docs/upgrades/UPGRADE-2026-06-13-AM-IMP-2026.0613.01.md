# UPGRADE-2026-06-13-AM-IMP-2026.0613.01

Project: HOZO AM

Version: `AM-IMP-2026.0613.01`

## Summary

OpenAI Codex CLI LLM backend: HOZO AM's judgment brain runs on `LLM_BACKEND=codex` — the OpenAI Codex CLI in headless exec mode, billed to the ChatGPT subscription. This is the HOZO side of the engine A/B test (SevenAM stays on Claude) with identical prompts, schemas, and calibration rules.

## Changes

- `src/llm-backend.js`: third pluggable backend `codex` — `runCodex` feeds the prompt via stdin, collects the final message via `-o` temp file, runs `--sandbox read-only --skip-git-repo-check`, and strips `OPENAI_API_KEY` from the child env so auth always uses the ChatGPT login (`codex login`, credential in `~/.codex`). `codexSelfTest` verifies availability.
- `scripts/local-worker.js`: backend selector `HOZO_WORKER_LLM_BACKEND` (default `codex`) drives the startup self-test, recovery retests, and the `LLM_BACKEND` passed to child jobs.
- `.env`: `LLM_BACKEND=codex` so direct script runs also use Codex. Optional `CODEX_MODEL` overrides the model.
- Codex CLI 0.139.0 installed globally via npm on the worker machine; ChatGPT account already authenticated.

## Environment Variables (names only)

LLM_BACKEND, CODEX_MODEL (optional), HOZO_WORKER_LLM_BACKEND (optional)

## Data Isolation Check

Backend change only; HOZO Notion sources unchanged. No Anthropic key present anywhere in HOZO.

## Verification

`codexSelfTest` pong passes; a full project-proposal analysis ran end-to-end on the codex backend against live HOZO data (26s, schema-compliant JSON, sound Chinese reasoning); worker runs in production with `backend=codex`.

## Status

Deployed on the worker machine (2026-06-12 night). Repo commit b91ade3.
